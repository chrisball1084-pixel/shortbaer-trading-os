"use client";
import { useRef, useState } from "react";
import { Download, HardDrive, RotateCcw, ShieldCheck, Upload } from "lucide-react";
import { useStore } from "@/lib/store";
import { Card, PageHeader, SectionTitle } from "../ui";
export function SettingsView(){const {data,reset,exportData,importData}=useStore();const input=useRef<HTMLInputElement>(null);const [status,setStatus]=useState("");const download=()=>{const blob=new Blob([exportData()],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`shortbaer-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)};const upload=async(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(file)setStatus(importData(await file.text())?"Backup importiert":"Ungültiges Backup")};return <div className="page"><PageHeader eyebrow="LOKAL & PRIVAT" title="Einstellungen" description="Daten sichern, wiederherstellen und die Grenzen des MVP verstehen."/><div className="settings-grid"><Card><SectionTitle>Daten</SectionTitle><div className="setting-row"><div className="setting-icon"><HardDrive/></div><div><strong>Lokaler Speicher</strong><p>{data.candidates.length} Kandidaten · {data.journal.length} Journal-Einträge</p></div></div><div className="button-row"><button className="primary" onClick={download}><Download size={17}/> Backup exportieren</button><button className="secondary" onClick={()=>input.current?.click()}><Upload size={17}/> Importieren</button><input ref={input} hidden type="file" accept="application/json" onChange={upload}/></div>{status&&<p className="status-message">{status}</p>}</Card><Card><SectionTitle>Sicherheit & Abgrenzung</SectionTitle><div className="security-list"><p><ShieldCheck/> Keine Brokerverbindung oder Orderausführung</p><p><ShieldCheck/> Keine Zugangsdaten oder externen Bezahl-APIs</p><p><ShieldCheck/> Keine Anlageberatung oder automatischen Signale</p><p><ShieldCheck/> Demodaten sind als DEMO markiert</p></div></Card><Card><SectionTitle>PWA installieren</SectionTitle><p className="helper">Auf dem iPhone in Safari öffnen, „Teilen“ wählen und „Zum Home-Bildschirm“ antippen.</p></Card><Card><SectionTitle>Demo zurücksetzen</SectionTitle><p className="helper">Ersetzt alle lokalen Änderungen durch den Auslieferungszustand.</p><button className="danger" onClick={()=>confirm("Alle lokalen Daten wirklich zurücksetzen?")&&reset()}><RotateCcw size={17}/> Lokale Daten zurücksetzen</button></Card></div></div>}
